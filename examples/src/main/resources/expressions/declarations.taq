axiom bcd (b,c,d) { 29.2, true, decimal 1234.56 }

template sample
{
  integer a,
  double b,
  boolean c
}
(
  a ? a == 0,
  b ? b == 29.2,
  c ? c == true,
  d
)

query<axiom> query_sample(bcd:sample)